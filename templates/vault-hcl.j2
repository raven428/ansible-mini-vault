#
# {{ ansible_managed }}
#
{%- set def = {
  'ui': 'true',
  'listener1': {
    'name': 'listener',
    'value': 'tcp',
    'params': {
      'tls_disable': '1',
      'address': '127.0.0.2:8200',
    },
  },
  'storage': {
    'value': 'raft',
    'params': {
      'path': '/var/lib/hashicorp-vault',
      'test': '425',
    },
  },
} -%}
{%- macro render_block(name, data, indent=0) -%}
  {%- set space = '  ' * indent -%}
  {%- if data is mapping and 'value' in data and 'params' in data -%}
    {{ '\n' }}{{ space }}{{ data.name | default(name) }} "{{ data.value }}" {
    {%- for k, v in data.params.items() -%}
      {{ render_block(k, v, indent + 1) }}
    {%- endfor -%}
    {{ '\n' }}{{ space }}}
  {%- else -%}
    {%- if data is mapping and 'value' in data -%}
      {%- set key = data.name | default(name) -%}
      {%- set value = data.value -%}
    {%- else -%}
      {%- set key = name -%}
      {%- set value = data -%}
    {%- endif -%}
    {%- if value is not none -%}
      {{ '\n' }}{{ space }}{{ key }} = {{ value if value in ['true', 'false'] or
      value is number or value.isdigit() else '"' ~ value ~ '"' }}
    {%- endif -%}
  {%- endif -%}
{%- endmacro -%}
{%- for key, value in (def | combine(v.over | default({}), recursive=True)).items() -%}
  {{ render_block(key, value) }}
{%- endfor %}
